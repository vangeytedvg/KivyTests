<MyLayout>
    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 20

        Label:
            id: my_label
            text: "Wazaaa"
            font_size: 32

        Switch:
            active:
            on_active: root.activated(self, self.active)